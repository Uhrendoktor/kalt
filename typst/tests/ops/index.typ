// Index tests for kalt bindings

#import "../../lib.typ": comp, map
#import "../utils.typ": binary_operation

#binary_operation(
  "Index",
  [index],
  (m, index) => { $ #m#index $ },
  (v1: $mat(1, 2; 3, 5)$, v2: $[0, 0]$, e: $mat(1)$),
  (v1: $mat(1, 2; 3, 4)$, v2: $[:, :]$, e: $mat(1, 2; 3, 4)$),
  (v1: $mat(1, 2; 3, 4)$, v2: $[:, 0]$, e: $mat(1; 3)$),
  (v1: $mat(1, 2, 3; 3, 4, 5; 4, 5, 6)$, v2: $[[0], :]$, e: $mat(1, 2, 3)$),
  (v1: $mat(1, 2, 3; 3, 4, 5; 4, 5, 6)$, v2: $[[0, -1, -2], 0:-2]$, e: $mat(1; 4; 3)$),
  (v1: $mat(1, 2, 3; 3, 4, 5; 4, 5, 6)$, v2: $[::2, ::2]$, e: $mat(1, 3; 4, 6)$),
  (v1: $mat(1, 2, 3; 3, 4, 5; 4, 5, 6)$, v2: $[::-1, ::-1]$, e: $mat(6, 5, 4; 5, 4, 3; 3, 2, 1)$),
  (v1: $mat(1, 2, 3; 3, 4, 5; 4, 5, 6)$, v2: $[1:2, 1:2]$, e: $mat(4)$),
  (v1: $mat(1, 2, 3; 4, 5, 6; 7, 8, 9)$, v2: $[0:3:-1, :]$, e: $mat(7, 8, 9; 4, 5, 6; 1, 2, 3)$),
  (v1: $mat(1, 2, 3; 4, 5, 6; 7, 8, 9)$, v2: $[:, 0:3:-2]$, e: $mat(3, 1; 6, 4; 9, 7)$),
  (v1: $mat(7)$, v2: $[:, :]$, e: $mat(7)$),
)
